You receive one image file, but within the file, there are two distinct images (first image and second image) separated by a blank stripe. (The upper image would be refered to as "the first image". The lower image would ber efered to as "second image"). Briefly describe each image.